<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--
        Cross-platform local play: mDNS discovery plus a TCP socket on the local network.

        NSD needs no runtime permission, and in particular needs neither location nor
        NEARBY_WIFI_DEVICES — those are for Wi-Fi Aware and Wi-Fi Direct, which this doesn't use.

        CHANGE_WIFI_MULTICAST_STATE and ACCESS_WIFI_STATE are for the multicast lock: many devices
        drop multicast traffic to save power unless one is held, and discovery then silently finds
        nothing at all.
    -->
    <!--
        Haptics. Without this every `Vibrator.vibrate` call is dropped on the floor — no crash, no
        log, just a game that never buzzes — which is exactly how this went unnoticed: the settings
        toggle, the degradation ladder and the patterns were all built and none of it could fire.
    -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />

    <application
        android:allowBackup="true"
        android:enableOnBackInvokedCallback="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PairForTwo">
        <!--
            Landscape only. The table is designed for it: the Swift's own comment is "Landscape:
            top ~1/3 is the scoreboard… bottom ~2/3 is the shared play area", and every iOS preview
            is landscapeLeft. In portrait the bands stretch and the layout reads as mostly empty.

            sensorLandscape rather than landscape, so the device can be held either way up — which
            matters for a game two people pass between them.

            Caveat worth checking on real hardware: from Android 16, apps targeting SDK 36 have
            orientation restrictions *ignored* on large screens (smallest width >= 600dp). So this
            holds on phones and foldables while closed, but a tablet may still rotate to portrait.
        -->
        <!--
            Handle the size and posture configs ourselves rather than being recreated.

            Found by folding a Pixel 9 Pro Fold mid-game: unfolding moves the app to the other
            display, Android recreates the activity, and the running game — held in composition —
            goes with it, dropping both players back to the menu. Compose re-reads the configuration
            reactively and every layout here is computed from `BoxWithConstraints`, so a resize
            needs no new resources; being recreated bought nothing and cost the game.
        -->
        <activity
            android:name=".MainActivity"
            android:configChanges="screenSize|smallestScreenSize|screenLayout|orientation|density"
            android:exported="true"
            android:label="@string/app_name"
            android:screenOrientation="sensorLandscape"
            android:theme="@style/Theme.PairForTwo.Splash"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>